# .coveragerc - Configuração de coverage para RC Gestor
# Mede apenas o "App Core" oficial (src + adapters + infra + data + security)

[run]
# Fontes do App Core a serem medidas:
# - src: núcleo da aplicação
# - adapters: integrações (ex.: Supabase Storage)
# - infra: camada de infraestrutura (clientes, net, settings, etc.)
# - data: tipos de domínio compartilhados e repositórios
# - security: criptografia de senhas e afins
source =
    src
    adapters
    infra
    data
    security

# Habilitar branch coverage para métricas mais completas
branch = True

[report]
# Mostrar linhas não cobertas no terminal
show_missing = True

# Precisão de 1 casa decimal
precision = 1

# Omitir arquivos não relevantes para coverage de produto
omit =
    tests/*
    */tests/*
    venv/*
    .venv/*
    */site-packages/*
    devtools/*
    scripts/*
    helpers/__init__.py
    sitecustomize.py
    main.py

[html]
# Configuração para relatórios HTML (se gerados no futuro)
directory = htmlcov
